/**
 * Движок расчёта стоимости поездки.
 *
 * Пакет намеренно не знает ни о React, ни о базе данных, ни о вебе: его
 * одинаково вызывают сайт, Telegram-бот, встраиваемый виджет и мобильное
 * приложение. Всё, что приходит извне, передаётся аргументами.
 *
 * Сейчас здесь только контракт — входная схема и типы результата (эпик E1).
 * Сама формула расчёта появится в E6, когда в базе будут реальные цены.
 */

export * from "./types";
